# railway.yml

services:
  - mongodb

environments:
  production:
    secrets:
      MONGODB_URI: mongodb+srv://bankiprudhviapp:pk143@cluster0.hv4xtww.mongodb.net/employpeeDB

buildCommand: yarn install
startCommand: yarn start
